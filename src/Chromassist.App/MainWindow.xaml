<Window x:Class="Chromassist.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Chromassist.App"
        mc:Ignorable="d"
        Title="{Binding AppTitle}"
        Height="720"
        Width="920"
        MinHeight="620"
        MinWidth="760"
        WindowStartupLocation="CenterScreen"
        Background="#FFF6F7F9">
    <Window.Resources>
        <local:ByteArrayToImageSourceConverter x:Key="ImageConverter" />
        <Style TargetType="Button">
            <Setter Property="Padding" Value="14,8" />
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="MinHeight" Value="38" />
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="MinHeight" Value="38" />
            <Setter Property="Padding" Value="8,5" />
        </Style>
    </Window.Resources>

    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,22">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="110" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="{Binding AppTitle}" FontSize="28" FontWeight="SemiBold" Foreground="#FF20242B" />
                <TextBlock Text="{Binding Subtitle}" Margin="0,5,0,0" FontSize="14" Foreground="#FF646B76" />
            </StackPanel>
            <ComboBox Grid.Column="1"
                      ItemsSource="{Binding Languages}"
                      SelectedItem="{Binding SelectedLanguage, Mode=TwoWay}"
                      VerticalAlignment="Center"
                      AutomationProperties.Name="Interface language" />
        </Grid>

        <Border Grid.Row="1" Background="White" BorderBrush="#FFDDE1E7" BorderThickness="1" CornerRadius="8" Padding="18" Margin="0,0,0,14">
            <StackPanel>
                <TextBlock Text="{Binding GameSection}" FontSize="17" FontWeight="SemiBold" />
                <ComboBox ItemsSource="{Binding Games}"
                          SelectedItem="{Binding SelectedGame, Mode=TwoWay}"
                          DisplayMemberPath="DisplayName"
                          Margin="0,12,0,8"
                          AutomationProperties.Name="Detected games" />
                <TextBlock Text="{Binding SelectedGame.StatusText}" Foreground="#FF565D68" TextWrapping="Wrap" Margin="0,0,0,12" />
                <StackPanel Orientation="Horizontal">
                    <Button Content="{Binding RescanLabel}" Command="{Binding ScanCommand}" />
                    <Button Content="{Binding BrowseLabel}" Command="{Binding BrowseCommand}" />
                </StackPanel>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Background="White" BorderBrush="#FFDDE1E7" BorderThickness="1" CornerRadius="8" Padding="18" Margin="0,0,0,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="220" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="{Binding PresetSection}" FontSize="17" FontWeight="SemiBold" />
                    <ComboBox ItemsSource="{Binding Presets}"
                              SelectedItem="{Binding SelectedPreset, Mode=TwoWay}"
                              DisplayMemberPath="DisplayName"
                              Margin="0,12,0,0"
                              AutomationProperties.Name="Colour vision preset" />
                </StackPanel>
                <StackPanel Grid.Column="1" Margin="24,0,0,0" VerticalAlignment="Bottom">
                    <TextBlock Text="{Binding SelectedPreset.Description}" TextWrapping="Wrap" Foreground="#FF404650" />
                    <TextBlock Text="{Binding ExperimentalNotice}" TextWrapping="Wrap" Margin="0,7,0,0" Foreground="#FF8A4B14" />
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="3" Background="White" BorderBrush="#FFDDE1E7" BorderThickness="1" CornerRadius="8" Padding="18" Margin="0,0,0,14">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Text="{Binding PreviewSection}" FontSize="17" FontWeight="SemiBold" Margin="0,0,0,12" />
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition Width="18" />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Text="{Binding OriginalLabel}" HorizontalAlignment="Center" Margin="0,0,0,7" />
                    <TextBlock Grid.Column="2" Text="{Binding AdjustedLabel}" HorizontalAlignment="Center" Margin="0,0,0,7" />
                    <Border Grid.Row="1" Background="#FF2E3138" CornerRadius="4" Padding="8">
                        <Image Source="{Binding OriginalPreview, Converter={StaticResource ImageConverter}}" Stretch="Uniform" SnapsToDevicePixels="True" />
                    </Border>
                    <Border Grid.Row="1" Grid.Column="2" Background="#FF2E3138" CornerRadius="4" Padding="8">
                        <Image Source="{Binding AdjustedPreview, Converter={StaticResource ImageConverter}}" Stretch="Uniform" SnapsToDevicePixels="True" />
                    </Border>
                </Grid>
            </Grid>
        </Border>

        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="{Binding StatusText}" VerticalAlignment="Center" TextWrapping="Wrap" Foreground="#FF4C535E" Margin="0,0,18,0" />
            <Button Grid.Column="1"
                    Content="{Binding ApplyLaunchLabel}"
                    Command="{Binding ApplyAndLaunchCommand}"
                    Padding="20,10"
                    FontWeight="SemiBold"
                    AutomationProperties.Name="Apply patch and launch game" />
        </Grid>
    </Grid>
</Window>
